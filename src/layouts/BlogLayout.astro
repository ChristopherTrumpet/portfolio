---
import Chat from "../components/Chat";
import Footer from "../components/Footer.astro";
import GrainyBackground from "../components/GrainyBackground.astro";
import Navbar from "../components/Navbar.astro";
import "../styles/global.css";

const { frontmatter } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, viewport-fit=cover"
    />
    <meta name="theme-color" content="#eee" />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <meta name="generator" content={Astro.generator} />
    <title>{frontmatter.title}</title>
  </head>
  <body>
    <div class="container flex flex-col max-w-3xl mx-auto h-dvh lg:px-0">
      <Navbar />
      <main
        class="flex flex-col items-center justify-between px-4 antialiased blog grow lg:px-0"
      >
        <article>
          <header>
            <p class="date">{frontmatter.date}</p>
            <h1>{frontmatter.title}</h1>
            <p class="description">{frontmatter.description}</p>
          </header>
          <hr class="mb-6 border-zinc-300" />
          <slot />
        </article>
        <a
          href="/blog"
          aria-label="Thank you for reading, return to blog archive"
          class="my-16 text-base text-center opacity-50 w-fit hover:opacity-100 no-underline!"
        >
          Thanks for reading...✌️
        </a>
      </main>
      <Footer />
    </div>
    <Chat client:load />
    <GrainyBackground />
  </body>
  <style>
    @import url("https://fonts.googleapis.com/css2?family=Lora:wght@400..500&display=swap");

    /* Post Title */
    .blog :global(h1) {
      font-family: Lora, var(--font-sans);
      font-size: var(--text-3xl);
      font-weight: var(--font-weight-medium);
      letter-spacing: var(--tracking-tight);
      line-height: var(--leading-tight);
      padding-bottom: 1rem;
    }

    /* Post Header */
    .blog :global(header) {
      margin-bottom: 1rem;
    }

    /* Description */
    .blog :global(.description) {
      color: var(--color-zinc-600);
      padding-bottom: 0;
    }

    .blog :global(.date) {
      font-family: var(--font-mono);
      font-size: var(--text-sm);
      font-weight: var(--font-weight-medium);
    }

    .blog :global(a) {
      text-decoration: underline;
      padding-bottom: 1px;
      text-underline-offset: 6px;
    }

    .blog :global(a):hover {
      color: var(--color-zinc-400);
    }

    @media (width >= 64rem) {
      .blog :global(header) {
        margin-top: 6rem;
      }
      .blog :global(h1) {
        font-size: var(--text-4xl);
      }
    }

    .blog :global(p) {
      font-family: var(--font-sans);
      font-size: var(--text-base);
      color: var(--color-zinc-700);
      line-height: var(--leading-normal);
      padding-bottom: 0.5rem;
    }

    .blog :global(h2) {
      font-family: Lora, var(--font-serif);
      font-size: var(--text-2xl);
      letter-spacing: var(--tracking-tight);
      margin: 1.5rem 0 1rem 0;
      padding-bottom: 0.5rem;
      border-bottom: solid 1px var(--color-zinc-300);
    }

    .blog :global(ul) {
      list-style-type: circle;
      padding-left: 1rem;
    }
  </style>
</html>
