---
import Layout from "../layouts/Layout.astro";
import { getAgentMessage } from "../lib/agent";

// const aiMsg = await getAgentMessage();
---

<Layout hasSidebar={false}>
  <div class="flex flex-col justify-center mx-auto max-w-xl h-dvh">
    <h1 class="font-bold tracking-tight">Gemini Model Message</h1>
    <p>Chat bot message...</p>
  </div>
</Layout>
